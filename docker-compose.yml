services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: dev-secret-key
      DATABASE_URL: sqlite:////app/instance/app.db
    volumes:
      - ./instance:/app/instance
